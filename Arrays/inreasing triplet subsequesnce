
nums=[1,2,3,4,5]
def increasing_triplet (nums):
    first=second=float("inf")
    
    for num in nums:
        if num <=first:
            first=num
        elif num <= second:
            second = num

        else:
            return True

    return False
print(increasing_triplet(nums))